# The Graph gateway API key (server-only) for real fee APR via subgraphs.
# Create one at https://thegraph.com/studio → API Keys (free plan: 100k queries/month).
# Copy this file to .env.local and fill in the key. Without it, APR falls back
# to the OHLCV volume estimate.
THEGRAPH_API_KEY=
